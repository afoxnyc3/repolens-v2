# Work Log: T04 — File filters (gitignore + custom patterns)
**Completed:** 2026-04-15T12:39:29.573945+00:00
**Status:** done

## Goal
Implement repolens/ingestion/filters.py. Functions: load_gitignore(repo_path), is_ignored(path, gitignore_rules, custom_patterns), is_binary(path), is_oversized(path, max_bytes=1_000_000). Use gitignore-parser for .gitignore support. Binary detection via null byte check on first 8KB.


## Acceptance Criteria
- .git/ directory is filtered out
- Files matching .gitignore patterns are filtered
- Binary files (e.g. PNG) are detected as binary
- Files over 1MB are flagged as oversized
- All tests in test_ingestion.py pass

## Validation (1/1 passed)

### ✅ `.venv/bin/pytest tests/test_ingestion.py -q`
```
....................                                                     [100%]
20 passed in 0.02s
```

## Notes
Implemented load_gitignore, is_ignored, is_binary, is_oversized in repolens/ingestion/filters.py. 20 tests passing. Uses gitignore-parser for .gitignore support, null-byte binary detection, stat-based size check. Always filters .git/ components.