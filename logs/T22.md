# Work Log: T22 — End-to-end smoke test on a real repo
**Completed:** 2026-04-16T21:24:07.379543+00:00
**Status:** done

## Goal
Run the full pipeline on a small real repo (this repo is fine). Ingest, classify, summarize (--scope all), build context bundle, and run an analyze task. Document the output in a smoke_test_output.md file. Verify costs are logged and the run shows as done.


## Acceptance Criteria
- All pipeline steps complete without error
- smoke_test_output.md contains the AI result
- repolens runs shows the run with status=done
- Total estimated cost is printed and reasonable (< $1 for a small repo)

## Validation (3/3 passed)

### ✅ `test -f smoke_test_output.md`

### ✅ `.venv/bin/python3 -m repolens.cli.main runs --limit 1 2>&1 | grep "done"`
```
1     repolens_v2_repo      analyze         claude-opus-4-7         done              808   $0.5765  2026-04-16 17:23
```

### ✅ `.venv/bin/python3 -m repolens.cli.main health 2>&1 | grep -i "ok\|healthy" || python -m repolens.cli.main list`
```
ID    Name                      Path                                               Files  Last scanned       
-------------------------------------------------------------------------------------------------------------
1     repolens_v2_repo          /Users/alex/dev/projects/repolens_v2_repo            106  2026-04-16 17:23
```

## Notes
End-to-end smoke against real Anthropic API. Probe verified ADR-004 caching: 9906 cache_creation_tokens on warm-up, 9906 cache_read_tokens on second probe within TTL. Real run analyze cost $0.5415 (under $1 ceiling). smoke_test_output.md persisted at repo root. Empirical finding: claude-opus-4-7 cache-write threshold is well above the documented 1024-token minimum; client now uses 2048 floor across all families.