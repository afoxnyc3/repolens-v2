# Work Log: T05 — Directory scanner
**Completed:** 2026-04-15T15:29:40.303125+00:00
**Status:** done

## Goal
Implement repolens/ingestion/scanner.py. Function: scan_repo(path, custom_ignores=[]) returns a list of FileRecord (dataclass: repo_root, relative_path, extension, size_bytes, mtime, content_hash). Applies gitignore and custom filters. Skips binary and oversized files. Uses os.scandir for efficiency.


## Acceptance Criteria
- scan_repo on sample_repo returns expected file list
- .git/ and gitignored files are absent from results
- Binary files absent from results
- content_hash is a valid sha256 hex string
- scan_repo on this repo itself completes without error

## Validation (2/2 passed)

### ✅ `.venv/bin/python3 -c "from repolens.ingestion.scanner import scan_repo; files = scan_repo('.'); assert len(files) > 0, 'scan returned 0 files'; print(len(files))"`
```
55
```

### ✅ `.venv/bin/pytest tests/test_ingestion.py -q`
```
............................                                             [100%]
28 passed in 0.05s
```

## Notes
scanner.py: FileRecord dataclass + scan_repo via os.scandir. Filters: gitignore, .git/, binary, oversized. sha256 per file. 8 new scanner tests added (28 total). Scan of repo itself: 55 files.