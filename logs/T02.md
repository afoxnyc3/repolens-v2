# Work Log: T02 — SQLite schema and migration runner
**Completed:** 2026-04-15T12:37:58.264175+00:00
**Status:** done

## Goal
Implement repolens/db/schema.py. Create all tables (repos, files, summaries, context_bundles, runs, schema_version) with indexes. Write a migration runner that creates the DB on first use and applies any new migrations. DB path from config (REPOLENS_DB env or ~/.repolens/repolens.db).


## Acceptance Criteria
- Running schema.py creates the DB and all 6 tables
- Re-running schema.py is idempotent (no errors on existing DB)
- All indexes exist in sqlite_master
- schema_version table has one row with version=1

## Validation (2/2 passed)

### ✅ `.venv/bin/python3 -c "from repolens.db.schema import init_db; import tempfile, os; os.environ['REPOLENS_DB']=tempfile.mktemp(suffix='.db'); init_db()"`

### ✅ `.venv/bin/pytest tests/test_db.py -q`
```
.........................                                                [100%]
25 passed in 0.08s
```

## Notes
Implemented repolens/db/schema.py with init_db(), all 6 tables with IF NOT EXISTS, 4 indexes, schema_version seeded to 1, WAL mode, FK enforcement. Added get_schema_version() helper. 25 tests in tests/test_db.py covering table creation, idempotency, index existence, schema version, env var path, parent dir creation, column presence, FK cascade.