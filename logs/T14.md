# Work Log: T14 — Token counter
**Completed:** 2026-04-15T16:05:24.310627+00:00
**Status:** done

## Goal
Implement repolens/context/token_counter.py. Function: count_tokens(text) -> int. Uses tiktoken with cl100k_base encoding if available. Falls back to len(text) // 4 if tiktoken raises ImportError. Function: estimate_cost(prompt_tokens, completion_tokens, model) -> float (USD).


## Acceptance Criteria
- count_tokens("hello world") returns a positive integer
- Falls back gracefully if tiktoken is unavailable
- estimate_cost returns a float >= 0

## Validation (1/1 passed)

### ✅ `.venv/bin/python3 -c "from repolens.context.token_counter import count_tokens; n = count_tokens('test'); assert n > 0, 'count_tokens returned 0'; print(n)"`
```
1
```

## Notes
Implemented count_tokens with tiktoken cl100k_base encoding and len//4 fallback. Implemented estimate_cost with per-model pricing table for opus/sonnet/haiku, unknown model defaults to sonnet pricing. 12 tests all passing, full suite 206/206.