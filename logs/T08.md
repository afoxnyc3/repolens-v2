# Work Log: T08 — CLI classify command
**Completed:** 2026-04-15T16:43:50.173689+00:00
**Status:** done

## Goal
Implement `repolens classify <repo>` command. Runs classifier + scorer on all files in the repo, updates classification and importance_score in the files table. Prints a breakdown: count per category, top 5 files by score.


## Acceptance Criteria
- repolens classify 1 (or by path) exits 0
- Files table has non-null classification after classify
- Output includes category counts

## Validation (1/1 passed)

### ✅ `.venv/bin/python3 -m repolens.cli.main classify 1 2>&1 | grep -E "core|test|config"`
```
core           18  ##################
  config          4  ####
  test            7  #######
Top 5 files by importance score:
```

## Notes
classify command wired to classifier.py/score_file; resolves repo by int ID or path; upserts classification+importance_score per file; prints category counts + top 5 by score