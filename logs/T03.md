# Work Log: T03 — Data access layer (repository.py)
**Completed:** 2026-04-15T15:28:02.066236+00:00
**Status:** done

## Goal
Implement repolens/db/repository.py with CRUD functions for all tables. No raw SQL outside this file. Functions: create_repo, get_repo, list_repos, upsert_file, list_files, upsert_summary, get_summary, save_bundle, get_bundle, create_run, update_run, list_runs. All functions accept a sqlite3.Connection.


## Acceptance Criteria
- create_repo / get_repo round-trip works
- upsert_file with same path twice updates rather than duplicates
- upsert_summary replaces on (repo_id, scope, target_path) conflict
- create_run + update_run round-trip works
- All tests in test_db.py pass

## Validation (1/1 passed)

### ✅ `.venv/bin/pytest tests/test_db.py -q`
```
..............................................................           [100%]
62 passed in 0.21s
```

## Notes
Implemented all 12 repository functions. PATCH semantics on upserts (merge fields, keep existing). file_paths JSON-serialised in bundles. update_run auto-sets completed_at on terminal status. list_runs ORDER BY created_at DESC, id DESC for deterministic ordering on same-second inserts. 62 tests passing.